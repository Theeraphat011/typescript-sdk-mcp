# Flowise Environment Configuration

# Flowise Authentication
FLOWISE_USERNAME=admin
FLOWISE_PASSWORD=admin123

# MCP Server Configuration
# For Production mode (with Docker MCP server):
# MCP_SERVER_URL=http://mcp-server:4000/mcp

# For Development mode (MCP server running on host):
MCP_SERVER_URL=http://host.docker.internal:4000/mcp

# MCP Server Port (if running on host)
MCP_PORT=4000

# MCP API Key (optional - for authentication)
# If set, clients must send this key in the 'x-mcp-session' header
# If not set, authentication is bypassed (development mode)
MCP_API_KEY=your-secret-api-key-here
